# Coronary RWS Analyser - Backend Environment Configuration
# Copy this file to .env and adjust values as needed

# ============================================================================
# Segmentation Engine Configuration
# ============================================================================

# Default segmentation engine: "nnunet" or "angiopy"
# - nnunet: Custom dual-channel nnU-Net (ROI-based, requires model weights)
# - angiopy: Seed-guided U-Net + InceptionResNetV2
SEGMENTATION_ENGINE=angiopy

# nnU-Net model directory (required if using nnunet engine)
# Point to your nnUNet_results folder containing trained models
# nnUNet_results=/path/to/nnUNet_results

# ============================================================================
# Model Paths (Optional - for custom model locations)
# ============================================================================

# AngioPy model weights path (optional, uses default if not set)
# ANGIOPY_WEIGHTS=/path/to/modelWeights.pth

# ============================================================================
# Server Configuration
# ============================================================================

# Backend server host and port
HOST=127.0.0.1
PORT=8000

# Enable debug mode (set to 1 for development)
DEBUG=0

# ============================================================================
# GPU Configuration
# ============================================================================

# CUDA device index (use -1 for CPU)
CUDA_VISIBLE_DEVICES=0

# Enable mixed precision (FP16) for faster inference
USE_FP16=1
